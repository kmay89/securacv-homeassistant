# SecuraCV HACS mirror — working notes

> **Start with [`AGENTS.md`](AGENTS.md).** It's the whole brief for this
> repository: what is carried from the monorepo (never edit it here), what
> is mirror-owned (`README.md` above all — it is the HACS store page), the
> voice rules, and the checks to run before committing. This repo is small
> on purpose; the brief is the whole story.
>
> Development of the integration itself happens in
> [`kmay89/securaCV`](https://github.com/kmay89/securaCV) under
> `custom_components/securacv/` — its `CLAUDE.md` and `AGENTS.md` carry the
> full project rules.
